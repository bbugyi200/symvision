# symvision

`symvision` flags unused public Python functions and classes, private symbols
imported across modules, and private symbols unused in their defining module.

```console
uv tool install symvision
symvision src/your_package
```

The project currently targets Python 3.11 and newer.

